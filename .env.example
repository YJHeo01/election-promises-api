CUSTOM_GPT_API_KEY=change-me
DATABASE_URL=sqlite:///./election_promises.db
APP_NAME=Election Promise Context API
ENVIRONMENT=development
LOG_LEVEL=INFO
LOG_FORMAT=json

# If SQLite reports disk I/O errors inside a synced folder such as OneDrive,
# use a local non-synced path instead, for example:
# DATABASE_URL=sqlite:///C:/Users/Public/Documents/ESTsoft/CreatorTemp/election_promises_runtime.db
